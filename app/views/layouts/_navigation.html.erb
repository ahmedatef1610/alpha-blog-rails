<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">

    <a class="navbar-brand d-flex justify-content-between align-items-center" href="<%= root_path %>">
      <img class="d-inline-block" width="50" src="<%= asset_path("icon.png") %>" alt="icon">
      <span class="ms-2">ALPHA BLOG</span>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= @activeInNavber=="home" ? "active" : "" %>" href="<%= root_path %>">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= @activeInNavber=="about" ? "active" : "" %>" href="<%= about_path %>">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= @activeInNavber=="articles" ? "active" : "" %>" href="<%= articles_path %>">Articles</a>
        </li>
      </ul>
    </div>

  </div>
</nav>
